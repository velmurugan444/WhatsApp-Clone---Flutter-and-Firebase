class Contact {
  String profile, username, description, email, password, address, mobile;
  Contact(this.profile, this.username, this.description, this.email,
      this.password, this.address, this.mobile);
}
